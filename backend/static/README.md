# Static Assets Directory

To add your own custom logo to the SENTRA dashboard:

1. Place your logo image file in this `static/` directory.
2. Name the file `logo.png` (or `logo.svg`, `logo.jpg`, `logo.webp`).
3. Reload or restart the application.

Your custom logo will automatically appear in the top navbar!
